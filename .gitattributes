# 统一按 LF 存储与检出：项目同时在 Windows（本地开发）与 Linux（GitHub Actions / Cloudflare 构建）
# 上运行，若行尾被自动转成 CRLF，workflow YAML 与 shell 步骤容易出现难查的失败。
* text=auto eol=lf

# 二进制资源不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.woff binary
*.woff2 binary

# 矢量资源按文本处理（生成脚本会重写，diff 需要可读）
*.svg text
